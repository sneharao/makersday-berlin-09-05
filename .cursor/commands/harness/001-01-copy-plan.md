Copy over the current plan we are working on, to the exec plans directory, as per skill at `harness/dev-workflow/001_plan.md`.

Note the plan should be copied using the `mv` command, not re-generated.
